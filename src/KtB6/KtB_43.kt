package KtB6

/**
 * @Description
 * @Author winiymissl
 * @Date 2024-05-05
 * Kotlin 中的运算符重载是通过定义相应的函数来实现的，这些函数以特定的名称表示相应的运算符。Kotlin 中支持重载的运算符有限，它们包括：
 *
 * 一元运算符：
 * unaryPlus (+a)
 * unaryMinus (-a)
 * not (!a)
 * inc (a++)
 * dec (a--)
 * 二元运算符：
 * plus (+)
 * minus (-)
 * times (*)
 * div (/)
 * rem (%)
 * rangeTo (..)
 * 比较运算符：
 * equals (==)
 * compareTo (比较)
 * contains (in)
 * invoke (调用)
 * get (下标访问)
 * set (下标赋值)
 * 总的来说，Kotlin 中有大约20个左右的运算符可以进行重载。这些运算符的重载可以帮助你自定义类的行为，使得你的类更加灵活。
 */
enum class Direction {
    SOUTH,
    EAST,
    NORTH,
    WEST
}


fun main() {

}